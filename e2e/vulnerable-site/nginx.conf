server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location = /redirect-outside {
        return 302 http://outside.localhost.invalid/redirected;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
